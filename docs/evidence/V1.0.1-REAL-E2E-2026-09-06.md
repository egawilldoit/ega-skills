# V1.0.1 Real End-to-End Validation Evidence — 2026-09-06

This file preserves observed command/results from the post-release manual
validation of the public `v1.0.1` tag. It is evidence for the summary recorded
in `docs/RELEASE-NOTES-1.0.1.md`; it is not a replacement for CI or the frozen
contract tests.

This change is documentation-only. It does not amend V1 behavior, specs, tests,
tag contents, or the release artifact. Applicable frozen contracts remain
[`SPEC-004`](../specs/SPEC-004-Router-and-Resolution-Contract.md),
[`SPEC-005`](../specs/SPEC-005-Project-Config-and-Lockfile.md), and
[`SPEC-006`](../specs/SPEC-006-MCP-Runtime-Contract.md).

## Release identity

The captured fresh-clone command sequence included `git checkout v1.0.1`, and
Git reported the tag switch and resulting HEAD:

```text
Note: switching to 'v1.0.1'.
HEAD is now at cb6a9ae Merge pull request #63 from egawilldoit/abmortaki/ega-617-v101-release-version-bump-docs-notes-tag
=== HEAD ===
cb6a9ae890487e12796bf91c4627d132e86e5e27
=== STATUS ===
=== VERSION FROM PACKAGE ===
1.0.1
```

This proves the observed checkout of `v1.0.1` resolved to the recorded release
commit in this manual session. The tag itself was not moved by this docs update.

## Install, build, CLI

Observed:

```text
pnpm install --frozen-lockfile  -> PASS
pnpm build                      -> PASS
CLI --version                   -> 1.0.1
CLI help                        -> included `ega-skills lock [<project-dir>] [--refresh]`
```

`pnpm` emitted its normal warning that `better-sqlite3` build scripts were
ignored, but the subsequent real registry/import path worked successfully,
showing SQLite was operational on this host.

## Real third-party corpus import

Source:

```text
mattpocock/skills
commit 3cca18b368ae95cdbdebbff572ccafa662551015
```

Observed import result:

```json
{"imported":37,"unchanged":0,"failed":0,"failures":[]}
```

## Fresh project lock flow

`ega-skills init /tmp/ega-v101-project` produced:

```yaml
schema_version: 1
routing:
  mode: suggest
  max_skills: 3
  max_tokens: 5000
namespaces:
  allow: []
  deny: []
skills:
  prefer: []
  deny: []
locking:
  required: true
```

`ega-skills lock /tmp/ega-v101-project` produced a lock containing all 37
imported skills. No config hand-edit was performed.

The first real resolve:

```text
task: I want to use TDD to build a feature test-first.
selected: mattpocock/tdd
tier: B
confidence: MEDIUM
routerImplementationVersion: 1.0.1
lockStatus: LOCKED
budgetStatus: WITHIN_BUDGET
```

## Lock refresh observation

Exact command used with no intervening registry change:

```text
export EGA_SKILLS_HOME=/tmp/ega-v101-real-home
node ~/ega-skills-v101-real-test/packages/cli/bin/ega-skills.mjs lock \
  /tmp/ega-v101-project \
  --refresh
```

Observed result:

```json
{"path":"/tmp/ega-v101-project/.egaskills.lock","created":false,"diff":{"added":[],"removed":[],"changed":[]},"skills":37}
```

This is a real no-op refresh observation. Deterministic lock serialization is
proved by the automated frozen contract/tests, not by this single manual run.

## Source removal / local-first persistence

The original corpus checkout was removed:

```text
rm -rf /tmp/mattpocock-v101-real
```

After removal, `ega-skills inspect mattpocock/tdd` still returned the immutable
version:

```text
sha256:17b3e6d1c9f7a40a6a2f3d3ed14ca64bf9023cafd758bc25f7a1278894559ccb
```

and still manifested:

```text
SKILL.md
agents/openai.yaml
mocking.md
tests.md
```

## OpenCode MCP connectivity

Observed from the real project:

```text
●  ✓ ega-skills connected
   /home/ubuntu/.nvm/versions/node/v24.18.0/bin/node
   /home/ubuntu/ega-skills-v101-real-test/packages/mcp/bin/ega-mcp.mjs
```

## Companion retrieval after source deletion

Prompt constrained OpenCode to the EGA Skills MCP only. Tool trace:

```text
ega-skills_inspect {"skill_id":"mattpocock/tdd"}
ega-skills_get_content {
  "skill_id":"mattpocock/tdd",
  "version_hash":"sha256:17b3e6d1c9f7a40a6a2f3d3ed14ca64bf9023cafd758bc25f7a1278894559ccb",
  "level":"L2",
  "max_tokens":2000,
  "file_path":"tests.md"
}
```

Observed result:

```text
Retrieval succeeded via ega-skills MCP only.
tokens=517/2000
first heading: # Good and Bad Tests
```

The original source directory was already absent when this succeeded.

## Natural client discovery

A prompt asked for the project's TDD skill guidance without naming EGA, MCP, or
any tool. In the clean constrained proof, OpenCode naturally used:

```text
ega-skills_search
ega-skills_resolve
ega-skills_get_content
```

The first L1 request failed because the real skill has no authored L1:

```text
E_CONTENT_LEVEL_MISSING
```

OpenCode then requested L2 successfully and produced the workflow from the
retrieved skill. The prompt explicitly prohibited shell commands and filesystem
reads, and none were used in that clean proof.

## User-only invocation semantics

Automatic route task:

```text
Use to-spec for this rough feature idea.
```

Observed:

```text
mattpocock/to-spec: Tier B candidate
versionHash: sha256:abfbefcd66d260b31389cebbd2bb9fe11e4121a48a388b0e3cc040d0033733fb
reasons include USER_INVOCATION_ONLY
selected: []
automaticSelectedTokens: 0
```

Explicit command:

```text
node ~/ega-skills-v101-real-test/packages/cli/bin/ega-skills.mjs resolve \
  --project /tmp/ega-v101-project \
  --task "Turn this rough feature idea into a specification." \
  --explicit mattpocock/to-spec
```

Observed explicit row:

```text
id: mattpocock/to-spec
versionHash: sha256:abfbefcd66d260b31389cebbd2bb9fe11e4121a48a388b0e3cc040d0033733fb
tier: E
reasons: EXPLICIT_USER, LOCKED_VERSION
explicitSelectedTokens: 632
```

The manual result shows the same version identity in the automatic candidate
and explicit row and that the resolver marked the explicit row
`LOCKED_VERSION`. It does not independently reconstruct or re-hash the lock
file. Explicit-selection and active-lock semantics remain governed by SPEC-004
and SPEC-005.

## Companion traversal rejection

OpenCode attempted exact companion retrieval with:

```text
file_path: ../tests.md
```

Observed guards:

```text
L1 + file_path -> E_MCP_INPUT_INVALID
L2 + ../tests.md -> E_CONTENT_FILE_UNKNOWN
```

No normalized traversal path was accepted and no outside file was disclosed.

## Minor real-client ergonomics observation

On some first attempts OpenCode removed the required `sha256:` prefix from the
version identity. EGA rejected those requests safely:

```text
E_VERSION_NOT_LOCKED
E_MCP_INPUT_INVALID
```

The client recovered by using the canonical hash returned by EGA. No wrong or
unlocked version was served.

## Result

**PASS for this real-user session.** The released V1.0.1 build completed the
fresh-user flow across release checkout, frozen install/build, real corpus
import, project lock UX, routing, no-op lock refresh, source-independent cache
retrieval, OpenCode MCP use, natural skill discovery, user-only invocation
semantics, and path-traversal rejection. Determinism and cross-platform
guarantees continue to come from the automated frozen contract/test suite and
Ubuntu + Windows CI.
