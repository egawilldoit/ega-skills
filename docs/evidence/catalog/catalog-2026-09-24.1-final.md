# catalog-2026-09-24.1 — final evidence

## Software identity (unchanged)

- Software: EGA Skills **2.0.0**; tag `v2.0.0` → `065421c03909089eb6077c1d285af24121329862`; tag object `9b38f3d0…` — untouched, immutable.
- Runtime-code freeze: candidate diff touches only `packages/mcp/artifact/**`, `docs/evidence/catalog/**`, `scripts/catalog-routing-runner.mjs`, and the single catalog-content test fixture (`tests/oauth/jwt-containment.test.mjs` — the JWT test fixture previously asserted the old catalog; semantics unchanged). No `packages/*/src`, no lockfile, no CI, no version bumps, no `v2.0.1`.

## Catalog identity

- Catalog ID / tag: `catalog-2026-09-24.1` (annotated tag object `c2379e78d85947ab90974139003fa3a5353e51d3`)
- Catalog release SHA: `22d58320ed9eb5f11759ef7a9f4423c9b165f083` (= `release/2.0` tip after merge of PR #115)
- Catalog release digest: `sha256:55b9dba5e0274dc0640c742a8f2ceab2889c1ef312ca16d1b4f9387cb30f752f` (hub `personal`, 48 skills)

## Source pins (immutable; upstream mains equalled every pin at execution start — no drift)

- `mattpocock/skills` @ `c55ee46073ed923f86ce59a5eb3b6d895095d1b7` — 25 skills, MIT
- `anthropics/skills` @ `34040c9c568585f6929bedeaad110ad08f079624` — 14 licensed skills (per-skill Apache-2.0 LICENSE.txt at pin)
- `vercel-labs/agent-skills` @ `063bee94c3f4df8453406c830b0a7df0f2860278` — 9 skills, MIT (README `## License`, repo-scoped per amendment)

## Inventory

- Discovered: 53 (Matt 25 / Anthropic 19 / Vercel 9) — exact
- Active: **48** (25 + 14 + 9); old Cursor skills: 0; ZIP duplicates: 0
- Excluded: 5 — `docx`/`pdf`/`pptx`/`xlsx` = EXCLUDED_LICENSE (proprietary, never staged/exported); `doc-coauthoring` = EXCLUDED_NO_LICENSE (amendment; never imported/staged/persisted/exported/served)
- Unknown licenses: 0; unknown provenance: 0

## Provenance (adoption architecture — platform-mandated, verified)

- All three sources adopted as **local-owned EGA trees** from pinned-clone-derived staging trees because `ega.yaml` routing metadata is required inside each namespace, and the platform namespace model forbids mixing an adopted Git source with owned trees in one namespace.
- **Byte-identity audit: 603/603 unpatched artifact files are byte-identical to their pinned upstream blobs** (per-file `git show <pin>:<path>` comparison; R1-verified). sources.lock/tree/snapshot digests bind the exact adopted trees; the operator receipts live in the Hub's `.intake-provenance/`.
- 5 documented minimal repairs (license-permitted, review-bound; digests in artifact PROVENANCE.md): `anthropic/claude-api` description trim (SPEC-001 1024 code-point limit; original `8227f0d1…` → repaired `3e85b0cc…`), 4 Vercel `name:` fields aligned to the brief-mandated EGA logical IDs (SPEC-001 directory-name rule; original/repaired digests recorded per file).
- `mattpocock/code-review` and `mattpocock/grilling` are hash-identical to the previous catalog (pure fresh intake). `mattpocock/tdd` version updated solely by the documented `ega.yaml` metadata file (SKILL.md bytes unchanged — never mutate bytes to manufacture hashes).

## EGA routing metadata (metadata only; reasons per skill)

- `mattpocock/diagnosing-bugs` triggers [never exits, hangs, root cause]
- `mattpocock/tdd` triggers [test-first, red green refactor]
- `anthropic/claude-api` anti-triggers [gemini, openai, gpt, langchain, langchain_openai, ollama, mistral, cohere, llama]
- `anthropic/mcp-builder` triggers [build an mcp server, implement an mcp server, mcp server exposing]; anti-triggers [diagnose, fails at startup, crash, existing mcp server]
- `vercel/react-best-practices` triggers [unnecessary rerenders, render waterfall, slow page]
- `vercel/deploy-to-vercel` triggers [deploy my app, give me the link, push this live]; anti-triggers [access token, cache]
- `vercel/vercel-cli-with-tokens` anti-triggers [dashboard]

## Quality / security / portability

- Deterministic intake quality: 48/48 VALID, 0 errors, 0 warnings, zero-failure import (three plans).
- Security: three parallel source agents (static inspection only) — P0 = 0, P1 = 0; HIGH_ACTION/SENSITIVE_CREDENTIAL skills retained with narrow, user-directed trigger scopes (see security evidence).
- Portability: 0 BLOCKED; REQUIRES_EGA_METADATA/REQUIRES_DERIVATIVE/HOST_SPECIFIC classes documented per skill.

## Routing

- Corpus: **323 cases (288 base + 35 collision; minimum 318)**, run through the real deterministic search + resolver against the real hub build — **323/323 PASS, 0 mandatory failures**.
- Live staging spot-checks (8 sampled intents): 8/8 match the corpus verdict.

## Approvals

- 48 approved / 0 rejected / 0 unapproved (review-batch records bound to candidate digests + revision).

## Artifact validation + isolation

- `validate-artifact.mjs`: OK — digest `sha256:55b9dba5e0274dc0640c742a8f2ceab2889c1ef312ca16d1b4f9387cb30f752f`, hub `personal`, 48 skills.
- Source-independent serving: all four tools PASS with no upstream clones present.
- Read-only proof: 611 artifact files byte-unchanged across MCP testing.
- Exclusions unretrievable (direct + staging + production inspect on all 7 excluded/old IDs → controlled errors).

## Staging (preview: `ega-skills-r3n6fnmec…` → Ready)

- Ignored-build environment replicated stale quirk verified: staging served the exact candidate artifact; hosted-smoke 17/17 PASS; mcp-probe identity anchors PASS (probe's default `search 'architect'`/`resolve 'design the architecture…'` candidate assertions are phase-agnostic defaults designed for `cursor/architect` and do not apply to the new catalog's representative — identity checks (version/digest/bytes/digest) PASS).
- Real-client note: Vercel preview SSO blocks real-client OAuth on previews (accepted 2.0 precedent); real clients accepted against production post-deploy.

## Reviewers

- R1: PASS (P0=0, P1=0, P2=2 — one fixed in place, one architecture note; 603/603 byte-identity audit independently re-executed). Executed by the main coordinating agent (reviewer subagent spawn was cancelled by the harness — deviation recorded).
- R2: PASS (P0=0, P1=0, P2=0, P3=3 notes). Same-mode deviation recorded.
- P0 = 0, P1 = 0 pipeline-wide.

## PR / CI / merge

- PR #115 (`catalog/2026-09-24-clean-reset` → `release/2.0`): required CI PASS (foundation ubuntu `107629530776`, foundation windows `107629531158`, contract-f `107629531186`; run `35998634001`).
- TOCTOU: base re-verified `065421c…` immediately before merge; merge SHA `22d58320ed9eb5f11759ef7a9f4423c9b165f083`.
- Production merge guard: `commandForIgnoringBuildStep=exit 0` applied BEFORE merge (merge-window build `ega-skills-bs0o3atps…` shows **Canceled by Ignored Build Step** — production untouched), restored to `null` after.

## Exact-SHA CI (catalog SHA 22d5832…)

- push: CI `35999975711` + hashing `35999975737` — foundation (ubuntu-latest) PASS, foundation (windows-2022) PASS, contract-f rls PASS, hashing traversal (ubuntu + windows) PASS.

## Catalog tag

- `catalog-2026-09-24.1` → `22d58320ed9eb5f11759ef7a9f4423c9b165f083` (annotated; tag object `c2379e78d85947ab90974139003fa3a5353e51d3`); proven on remote via `refs/tags/catalog-2026-09-24.1^{}`.

## Production

- Rollback target (captured before deployment): `dpl_2BpyKiQVYvb2Tv9xNVCxCUureSH8` (old catalog, digest `sha256:70a37c28…`) — retained Ready, never mutated or deleted.
- Deployment: pinned `gitSource` commit deployment at `22d5832…`; window 2026-09-24T12:46:10Z → 2026-09-24T12:46:44Z, terminal **Ready** (`ega-skills-84gp150cs-egas-projects-4fb87621.vercel.app`, now serving the canonical alias `https://ega-skills-mcp.vercel.app`).
- Infrastructure smoke: hosted-smoke **17/17 PASS** (`/healthz` 200, `/readyz` 200, both metadata paths, anonymous 401 + challenge, hostile Origin 403, unknown route 404, spoof resistance, invalid token 401, authenticated catalog).
- Production MCP: exactly four tools; `effective_release_digest` = `sha256:55b9dba5…`; all 7 excluded/old IDs confirmed unretrievable; representatives match artifact registry version hashes (diagnosing-bugs `f56e6a33…`, mcp-builder `039eac54…`, react-best-practices `31856c5e…`).
- Rollback: not triggered.

## Clients

- Real-client OAuth headless logins (production): Codex OAuth PASS (12/12 checks; DCR opaque-uuid, PKCE S256, resource pinned) and OpenCode OAuth PASS (12/12 checks; connected (OAuth)).
- OpenCode (1.18.32, isolated XDG, strict remote OAuth config `oauth:{}`): exactly four `ega-skills_*` tools; all four explicitly invoked; search shows new-catalog ids + digest `55b9dba5…`; get_content L2 for `vercel/react-best-practices` = 7244 bytes `sha256:74e45648…` = exact artifact bytes. PASS.
- Codex (codex-cli 0.156.0, isolated CODEX_HOME): `mcp list` connected (OAuth); `search` + `resolve` executed observing the new digest; `inspect`/`get_content` turns hit the provider account usage cap mid-run (operational quota blocker; not a catalog defect) — these two complete when the quota reset allows immediate re-execution against the same production endpoint, alongside the direct-probe identities which already PASS. (See final report for the completed re-run.)
- Cross-client parity anchoren: direct MCP probe (both), OpenCode production, Codex search/resolve all observe catalog digest `sha256:55b9dba5…` and the identical representative version hashes above.

manual post-release validation: not part of this execution


## Codex production completion (post quota-reset re-run)

- search → `anthropic/mcp-builder @ 039eac54…` + digest `sha256:55b9dba5…`
- resolve → completed with effective digest
- inspect → `039eac54…` version hash
- get_content → structuredContent.content = exact artifact bytes (9092 bytes, `sha256:0f4592dc…295`) — EXACT MATCH
- Cross-client parity final: direct probe / Codex / OpenCode all observe catalog digest `55b9dba5…`, identical representative version hashes and content digests. No mismatch.
