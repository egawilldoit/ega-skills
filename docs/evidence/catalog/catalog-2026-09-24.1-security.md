# catalog-2026-09-24.1 — supply-chain security review

Method: three parallel source agents statically inspected every discovered directory (SKILL.md, scripts, references, rules, resources, lib, agents configs) at the pinned commits. No skill script was executed; no credentials used; no production systems touched.

## Per-source summary

### mattpocock (25)
- Risk: LOW 16, MEDIUM 8, SENSITIVE_CREDENTIAL 1 (wizard), HIGH_ACTION 0
- Network behavior: browser-side CDN loads (Tailwind/Mermaid docs pages); gh/glab tracker API calls in tracker-dependent skills; research delegates web reading to a background agent.
- Notable: wizard generates bash wizards that capture secrets (hidden input, gh auth status gate, confirm gates, bash -n/shellcheck lint, no self-execution). Trigger scope is user-invoked and narrow (provisioning/credentials/CI-secrets/migration).
- No destructive commands (rm -rf, force push, git reset --hard) instructed in any of the 25.

### anthropic (14 active)
- Risk: LOW 5, MEDIUM 4, HIGH_ACTION 4 (mcp-builder, skill-creator, web-artifacts-builder, webapp-testing), SENSITIVE_CREDENTIAL 1 (claude-api: ant CLI credential flows)
- claude-api materializes bearer tokens only under explicit Anthropic CLI workflows; all instructions are user-directed, none self-exfiltrate.
- skill-creator's eval viewer can serve a localhost HTTP server (--static fallback documented).
- webapp-testing uses Playwright against LOCAL apps only.

### vercel (9)
- Risk: MEDIUM 4, HIGH_ACTION 1 (deploy-to-vercel), SENSITIVE_CREDENTIAL 1 (vercel-cli-with-tokens), LOW 3
- deploy-to-vercel: HIGH_ACTION — deployment actions are its declared purpose; ega.yaml triggers constrain it to explicit deploy intent, and the collision corpus proves generic Vercel questions do not select it.
- vercel-cli-with-tokens: token-hunting heuristic flagged by the security review; behavior is explicit in its stated purpose (token-based CLI). ega.yaml anti-trigger keeps dashboard/how-to questions away.
- All inspection static; no deployment or credential operations were performed during validation.

## Excluded-content control
- anthropic/docx, pdf, pptx, xlsx: NOT staged, NOT copied into the Hub, NOT exported (verified absent from the artifact inventory).
- anthropic/doc-coauthoring: never imported/staged/persisted/exported/served (verified absent).
- cursor/architect, cursor/setup-pstack: not part of the new catalog (verified absent).
- Vercel ZIP packaging duplicates (6) never became skill records.

## Verdict
P0 = 0, P1 = 0 security findings. High-action skills retained with narrow trigger scope enforced by routing metadata and proven by the collision corpus.
