{
  "name": "ega-skills",
  "version": "1.0.1",
  "private": true,
  "description": "EGA Skills Hub V1 — local-first deterministic agent-skill registry and resolver",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": "24",
    "pnpm": "10.0.0"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false --force",
    "test": "node --test",
    "contracts:check-a": "pnpm exec tsc -b --force packages/hashing && node scripts/contracts/validate-contract-a.mjs",
    "contracts:test-a": "pnpm exec tsc -b --force packages/hashing && node --test tests/contracts/contract-a.test.mjs",
    "contracts:check-b": "pnpm exec tsc -b --force packages/hashing && node scripts/contracts/validate-contract-b.mjs",
    "contracts:test-b": "pnpm exec tsc -b --force packages/hashing && node --test tests/contracts/contract-b.test.mjs",
    "contracts:check-c": "pnpm exec tsc -b --force packages/hashing && node scripts/contracts/validate-contract-c.mjs",
    "contracts:test-c": "pnpm exec tsc -b --force packages/hashing && node --test tests/contracts/contract-c.test.mjs",
    "contracts:check-d": "node scripts/contracts/validate-contract-d.mjs",
    "contracts:test-d": "node --test tests/contracts/contract-d.test.mjs",
    "contracts:check-e": "node scripts/contracts/validate-contract-e.mjs",
    "contracts:test-e": "node --test tests/contracts/contract-e.test.mjs",
    "specs:check": "node scripts/specs/check-specs.mjs"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
